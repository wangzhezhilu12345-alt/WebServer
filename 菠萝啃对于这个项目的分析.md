# HTTP高性能服务器项目解析

# 前置知识

## HTTP报头知识补充

看parse的头文件，设计了对于http命令的解析。
这是一个非常完整的http请求:
    Host = 0,
    User_Agent,
    Connection,
    Accept_Encoding,
    Accept_Language,
    Accept,
    Cache_Control,
    Upgrade_Insecure_Requests。所以在设计http请求类的时候，这些都带上

    那么这些变量是一定要保存的，作为一个Http请求
    方法:get/post
    版本号:ver
    唯一资源符:url
    请求的内容:content
    还剩下一个变长的报头

---

# 需要设计什么

## 1.HTTP服务器类

基本的服务器端口80，然后socket套接字那基本的几步。

## 2.解析HTTP请求类

对HTTP的请求进行解析，首先先设计一个http的解析的类，名字是httprequest，里面需要实现的接口

变量：method GET/POST等这些方法

​			path 	URL路径

​			

gpt：因为http报头的可变性很高，所以需要设计一个笼统的东西。

gpt推荐的是用map去取，具体我再想想，重要的字段就单独开变量存下来。

## 3.套接字封装

这个比较ez，设计一个套接字类，然后传入参数，调用系统接口绑定即可。

## 4. Http应答



# 状态机补充

这是一种高效处理用户逻辑的编程方法。一般用ENUM这个枚举我们需要处理的业务逻辑。
他为什么能这么快的处理信息呢？是因为用这个enum就是程序内部在记住的信息，在编译的时候就已经知道了，如果用string去比较的话会出现cpu炸掉的情况。

# 事件处理模式
一般就用reactor这个坐响应

# 服务器需要处理的事件
## IO请求
    具体可以详细说到客户端的请求链接
## 信号
    这个暂时没想到...记得要有对信号的一定的无视
## 定时器
    这个部分是我学的最差的部分了....真的没理解。







